<div class="card" style="width: 30rem;">
    <div class="card-header text-center">
        <h4>New Burgers</h4>
    </div>
    <div class="card-body">
        {{#each undevouredList}}
        <div>
            <p>({{id}}.) {{burger_name}}
                <button class="btn btn-sm btn-outline-secondary m-1 devour-btn float-right" data-burger="{{id}}">Devour<i
                        class="fas fa-utensils ml-2"></i>

                </button>
            </p>
        </div>
        {{/each}}

    </div>
</div>